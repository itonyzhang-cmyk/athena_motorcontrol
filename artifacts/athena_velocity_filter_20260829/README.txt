EXPERIMENTAL FIRMWARE - DO NOT USE AS THE DEFAULT RELEASE IMAGE

This image was built from a dirty worktree with N_POS_SAMPLES=128 to evaluate
AS5047 velocity filtering. It is retained only as historical test evidence.
The default WebUI and release configuration must not reference this artifact.
The reviewed motor-side release image is
../athena_motor_side_protocol_20260829/motorcontrol.bin.

Before flashing any image, verify its provenance and SHA-256 against the
current release record. Experimental images require an explicit, separate
test decision.
